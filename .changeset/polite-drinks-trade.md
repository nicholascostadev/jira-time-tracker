---
"@nicholascostadev/jira-time-tracker": minor
---

Add a worklog review step when stopping the timer so users can choose between posting a single entry or split entries by pause/resume segments. Default to single entry for uninterrupted sessions and split entries for sessions with multiple tracked segments.
